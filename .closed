# This is a simple config file for Closed.
# The first section is the default section.

[default]
# Just use the defaults.

[release]
# Use the LDC compiler and make a release build.
-c=ldc2
-b=RELEASE
